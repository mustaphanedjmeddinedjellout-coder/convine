/**
 * DrapeOpening — thin wrapper that delegates to the new WebGL CurtainReveal.
 * Kept as a separate file so all existing imports continue to work.
 */
export { default } from './CurtainReveal/CurtainReveal';
